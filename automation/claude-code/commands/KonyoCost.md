---
description: RETIRED. Use /Konyo <task> --reversible. There is no cheap-path door.
argument-hint: [do not use — run /Konyo --reversible]
---

# Retired

`/KonyoCost` is not a door. The one command is `/Konyo`.

Routine work, cheap to be wrong:

```
/Konyo <task> --reversible
```

Do not invoke the engine from this file. This command exists only so an old `/KonyoCost`
fails loudly instead of silently picking a cost name you no longer choose.
