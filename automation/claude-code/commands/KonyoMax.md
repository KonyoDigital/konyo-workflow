---
description: RETIRED. Use /Konyo <task> --irreversible. There is no max door.
argument-hint: [do not use — run /Konyo --irreversible]
---

# Retired

`/KonyoMax` is not a door. The one command is `/Konyo`.

Irreversible work (money, security, trading, migrations, live ships):

```
/Konyo <task> --irreversible
```

Do not invoke the engine from this file. This command exists only so an old `/KonyoMax`
fails loudly instead of silently picking a cost name you no longer choose.
