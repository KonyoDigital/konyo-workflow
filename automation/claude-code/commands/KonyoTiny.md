---
description: RETIRED. Use /Konyo and pass items:[{file,instruction}]. Tiny was a plan, not a door.
argument-hint: [do not use — run /Konyo with items[]]
---

# Retired

`/KonyoTiny` is not a door. The one command is `/Konyo`.

A known edit list was never a fourth quality. It is an argument. Pass the plan on the one door:

```
/Konyo <task>
```

and include `items:[{file, instruction, anchor}]` in the payload. That skips the architect at
any stakes.

Do not invoke the engine from this file. This command exists only so an old `/KonyoTiny`
fails loudly instead of silently picking a cost name you no longer choose.
