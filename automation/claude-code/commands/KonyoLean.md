---
description: RETIRED. Use /Konyo. This was the default costly shape — that is now just /Konyo with no flag.
argument-hint: [do not use — run /Konyo instead]
---

# Retired

`/KonyoLean` is not a door. The one command is `/Konyo`.

| You wanted | Run |
|------------|-----|
| daily ship, default costly shape | `/Konyo <task>` |
| cheap to be wrong | `/Konyo <task> --reversible` |
| cannot roll back | `/Konyo <task> --irreversible` |

Do not invoke the engine from this file. This command exists only so an old `/KonyoLean`
fails loudly instead of silently picking a cost name you no longer choose.
