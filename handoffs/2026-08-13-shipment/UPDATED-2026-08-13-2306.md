# Updated leftover board — 2026-08-13 23:06 IDT

Re-measure after the 22:10 shipment. **Read-only.** Engines and product trees were not edited this pass.

Compared against [`00-ENGINE-PARITY.md`](00-ENGINE-PARITY.md) / [`01-CLAUDE-SELF-FIX.md`](01-CLAUDE-SELF-FIX.md) / [`02-GROK-UPGRADE.md`](02-GROK-UPGRADE.md). Those files still describe **22:10**. This file is what is true **now**.

---

## Headline

They **did** work Claude’s shipper (v36 / v36.1). Install JS and repo JS are **byte-identical** again. The live d2r lock is **gone**. Grok’s implementer body **did not move**. `parity.sh` now watches `Konyo-Grok.rhai` (that was G6). Cheap proofs: 3 green, **1 red (stale)**, 1 dead file.

---

## Claude engine (C1–C8) — now

Live file: `~/.claude/workflows/konyo-workflow.js`  
**271036 B · md5 `8b21620c` · 3476 lines · mtime 22:48**  
Repo copy: **same hash.** `parity.sh` JS pair: **in sync.**  
`node --check` → **JS_PARSE_OK.**

| ID | 22:10 | 23:06 | Evidence |
|---|---|---|---|
| **C1** plan seat discarded | OPEN | **LANDED** (v36 + v36.1 tighten) | `:2065` assigns `planSeat`; veto requires `reached && severity==='blocking' && concerns.length && verdict!=='no concerns'`; then `bail()` before Build. |
| **C2** courier taught dead MCP key | OPEN | **LANDED** | `:530–534` says put the **actual** error; do not assume the key is dead. Diary at `:421` still names the old failure (correct). `known-dead key` **gone** from this file. |
| **C3** 0.2.118 pin + no perl wrapper | OPEN | **LANDED** | `:454–466` refuse to pin; live 1.0.3 named as measurement. `PERL_ALARM` is what `grokHow` runs (`:518–519`). |
| **C4** provenance = non-empty | OPEN | **LANDED**, then **v36.1 repaired a false-negative** | Regex / path / `GROK_CLI` / `~/.grok/bin/grok`; first cut would have refused real reviews (perl wrapper 243 chars, clip at 200). Comment `:612–627`. |
| **C5** `degraded` all-or-nothing | OPEN | **LANDED** | `TE_DEGRADED = reached < attempted`; `TE_PARTIAL`; payload ships counts (`:3148–3159`, `:3453–3461`). |
| **C6** ledger after SHIPPABLE | OPEN | **NARROW LAND** | Declarations moved **above** `SHIPPABLE` (`:3121`). Honest comment: SHIPPABLE still does **not** read those counts; silent eye must not block. Plan veto never reaches this line. |
| **C7** install vs repo description | OPEN | **DONE** | hashes equal. Uncommitted in git (`M automation/claude-code/konyo-workflow.js` + `M parity.sh`). |
| **C8** agent-army sibling | OPEN | **PARTIAL** | Install `agent-army.js` **221743 B** (22:31) has plan-seat assign + `blocker('THE THIRD EYE REFUSED THIS PLAN')` **and still** `0.2.118` + `known-dead key` (`:329`, `:402`). **Repo copy still 219602 / 2026-08-11.** Drift. Direction is public → install. |

**No v36 proof file exists.** C1–C6 are proven by reading the install file and `node --check`, not by a RED-then-GREEN harness. That is the remaining proof gap on Claude.

---

## Cheap proofs I actually ran (no browser — test-venue)

| Script | Result | What it means |
|---|---|---|
| `v32_ship_predicate_proof.mjs` | **72/72** | v33 pre_existing + v35 items[] refuse still hold on the new body |
| `v30_meter_routing_proof.mjs` | **PASS** | meter caps still present |
| `scar_capture_proof.mjs` | **12/12** | carve floor 3 still load-bearing |
| `v27_empty_plan_proof.mjs` | **FAIL 1** | Scenario 3 expects log `ARCHITECT SKIPPED\|caller items` on `{apply:false, items:[{file}]}`. v35 now **refuses that combo before** `:1744` logs the skip. Engine line still exists. **Stale assertion**, not a new empty-plan hole (scenarios 1–2 still get `architect_noop` / `architect_empty`). |
| `ceiling_proof.mjs` | **DEAD** | opens retired `~/.claude/workflows/konyo-workflow-max.js` (ENOENT). Instrument, not subject. |
| `ceiling_proof_v4.mjs` | **PASS** | ceiling holds, run can still report |
| `v27_grok_safeguard_proof.sh` vs `Konyo-Grok.rhai` | **ALL CONTRACTS PRESENT** | string contracts only; does not prove G1–G5 (those strings are not in the rhai) |

**To perfect the tests (not done — read-only):**

1. Rewrite v27 scenario 3 into two cases: `apply:false + items[{file}]` → v35 refuse; `apply:true + items[{file}]` → `ARCHITECT SKIPPED (caller items[])`.
2. Point `ceiling_proof.mjs` at the one body, or delete it (v4 already exists).
3. Add `v36_third_eye_proof.mjs` that stubs `thirdEyeAsk` / harness seats for C1 veto, C4 `echo hi` reject, C4 real-path accept, C5 1/4 degraded.

---

## Grok engine (G1–G7) — now

`Konyo-Grok.rhai` still **122487 B · `46a57951`** (same as 22:10). Body **not upgraded**.

| ID | 23:06 |
|---|---|
| **G1** items[] file-shape refuse | **STILL OPEN** — only task-prose heuristic `:716–740` |
| **G2** every abort releases lock | **STILL OPEN** — no `release_lock_if_held` helper |
| **G3** plan seat can block | **STILL OPEN** — stores text, Build always continues |
| **G4** render pass-with-failures / pre_existing | **UNVERIFIED** (loop not re-read this pass; body hash unchanged ⇒ same as 22:10) |
| **G5** tiny ship predicate | **UNVERIFIED** (hash unchanged) |
| **G6** proof + parity | **HALF** — `parity.sh` now has Konyo-Grok + proof pairs. **Repo proof.sh still 4821 vs install 5322** (the one remaining `parity.sh` DRIFT). Local konyo-workflow copy of the proof **is** in sync with install. |
| **G7** skill pointer | **STILL OPEN** (not checked beyond hash — skills still equal `dfb8b0dc`) |

`/konyo-workflow` remains the 2698 B redirect stub. Correct.

---

## Repos — delta since 22:10

Lock dir **empty**. The v1710 d2r lock expired or was released. Tree is **dirty** again (work happened).

| Repo | 22:10 | 23:06 | Left |
|---|---|---|---|
| **konyo-workflow** | clean, even | dirty 3: `M` JS, `M` parity.sh, `??` handoffs/ | **Commit** the v36 JS + parity (C7 durability). Do not commit handoffs unless you want them public. |
| **kai-achilles** | clean, **+38** | same | Push decision unchanged. |
| **achilles-revival** | dirty 3, even | dirty 11, **+23** (merged pt-card-polish, tip `c5722d4`) | Uncommitted worktrees, audit/scar notes, `mission_control.html`, leftover `.pre-*` files. |
| **d2r_bible_tests** | clean + **live lock** | lock gone; **dirty 9** (bible.html, 3 specs, control_app/ui, tv_diablo, WINDOWS_SHIP, untracked probe) | Finish or revert that console work. Do not run Playwright on the Mac. |
| **predicter** | dirty 4 | **same 4 CSS+CHANGELOG** | Untouched. |
| **Grok-MCP** | dirty 3 | **same** (loader uncommitted, tip April) | Still the C2 counterpart. |
| **karine-workflow-project** | dirty 3 | same | Untouched. |
| **clawd** | unborn, 123 | same | Not a repo. |
| edge-engine, routines, coin site, starter, facility, issta, fibi, tz | clean / no remote | unchanged | — |
| bridge_repo | even | new auto-sync `de31a2e` 22:45 | ignore |

`gh run list` for several remotes returned empty JSON this pass — **CI not established** (N/A), not “green”. predicter last CI: 2026-08-02 success on v396.

---

## What is left, in order

1. **Commit** konyo-workflow’s v36 JS + `parity.sh` (or they die at next reboot/sync story).
2. **Perfect the three proof holes** (v27 scenario 3, dead `ceiling_proof.mjs`, missing v36 harness). That is the “tests” leftover.
3. **Grok G1–G5** — still the whole Grok upgrade. Author at `~/.grok/workflows/Konyo-Grok.rhai`.
4. **Copy install `v27_grok_safeguard_proof.sh` → repo** (only remaining parity DRIFT).
5. **agent-army C8 remainder:** dead-key sentence + 0.2.118; then decide install vs public vs the stale repo copy.
6. **Human calls:** kai-achilles +38 push; predicter dirty CSS; Grok-MCP commit loader; d2r dirty console; achilles leftover worktrees.

## What this pass did not do

- Edit any engine or test (read-only).
- Run Playwright or any product suite on the Mac.
- Execute `/Konyo-Grok` or `/konyo-workflow`.
- Prove C1/C4 with a stubbed seat (no v36 proof file).
- Confirm d2r dirty files are good or bad — only that they exist.
- Public `konyo-workflow-grok` merge.

## Verdict

**DRAFT** on “everything they worked is sealed.”  
Claude C1–C7 **look landed** in the live install; **uncommitted**; **under-tested**.  
Grok body **unchanged**.  
One proof is **red for a stale reason**. That is an instrument fault, not a new empty-plan regression.

Mode: **SOLO**. No scar — the v27 red is the next run’s job if they ask for a write.
