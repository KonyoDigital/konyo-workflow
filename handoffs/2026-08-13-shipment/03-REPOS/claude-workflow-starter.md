# claude-workflow-starter

- **Path:** `/Users/konyo/claude-workflow-starter`
- **Tip:** `814382a` 2026-06-30 — bootstrap.sh
- **Tree:** clean, even with origin

## Upgrade / update

This is **six weeks** behind the live shipper (lean/tiny/max merge, v26–v35, Konyo-Grok). A starter that still describes a pre-merge world will train the next install wrong.

## Debug / fix summary

| Item | Evidence | Fix |
|---|---|---|
| Stale relative to `~/konyo-workflow` | tip 2026-06-30 vs engine 2026-08-13 | Either point README at `konyo-workflow` / `ship-skill` / `konyo-workflow-grok`, or delete claims that it is the current install. A stale starter is worse than none (same rule as the superseded `~/.konyo-workflow/SKILL.md`). |

Not opened beyond `git log -1` / status.
