# Leftover board — 2026-08-14 00:25+ IDT (after this write pass)

Achilles (`kai-achilles`, `achilles-revival`) was **read-only**. Everything else was fair game.

## What I fixed this pass

| Item | Where | Proof |
|---|---|---|
| v27 scenario 3 stale vs v35 | `automation/claude-code/v27_empty_plan_proof.mjs` | split 3a refuse / 3b apply+skip — **v27 PASS** |
| G1 items[] dry-run refuse | `Konyo-Grok.rhai` (install → 2 copies) | `v27_grok_safeguard_proof.sh` v36 block **ALL PRESENT** |
| G2 `release_held_lock` on every abort, including architect-fail | same | same |
| G3 plan seat schema + blocking complete | same | same |
| G4 `pass:true` + failures[] cannot PASSED | same | same |
| G6 proof contracts + copy sync | install proof.sh → repo + konyo-workflow copy | `parity.sh` **ALL LOCAL PAIRS IN SYNC** |
| G7 Grok skill pointer | `~/.konyo-workflow/SKILL.md` | read back |
| MCP loaders same override | `Grok-MCP/main.py` now `override=False` on both loads | `py_compile` OK |

Cheap proofs run (no browser): v27 PASS · v32 72/72 · v36 44/44 · v30 PASS · scar 12/12 · grok contracts ALL PRESENT.

G5 tiny shippable: already correct in the rhai (`max_only` completeness only). Not re-ported.

## Achilles (not touched)

| Repo | State |
|---|---|
| kai-achilles | clean, **+38** unpushed through v406 |
| achilles-revival | even with origin; dirty `POST_SHIP_AUDIT.md` + `kai_scars_pending.md` only |

## Still left (not bugs I could close without a human call)

| Item | Why I left it |
|---|---|
| konyo-workflow **uncommitted** (JS v36, rhai v36, proofs, parity, handoffs/) | You did not ask to commit. Disk is consistent. |
| **predicter** 4 dirty files (v397–v407 CSS + CHANGELOG, braces match, dead selector gone) | Finished-looking stamp, uncommitted since 2026-08-06. Commit is yours. |
| **Grok-MCP** dirty `main.py` + `utils.py` + `?? chats/` | Loader fix is real; I will not `git add` a secret-adjacent tree or `chats/` without you. |
| **agent-army.js** repo copy 219602 vs install 224901 | Direction is **public → install**. I did not copy install over the vendored repo file. Public still 218945. |
| **karine-workflow-project** dirty `validate_skill.py` (fold detection) | Looks like their Desktop work. Not this ship. |
| **clawd** unborn + 123 untracked | Not a repo. |
| **claude-workflow-starter** tip 2026-06-30 | Stale starter; rewriting it is a product decision. |
| Public `konyo-workflow-grok` 113859 vs stub | Merge, never copy. |

`d2r_bible_tests` is **clean** at `ace17d7` v1711. Lock dir empty.

## Verdict

**DRAFT** on “all leftover work sealed.” In-scope engine + proof holes I could close without Achilles or a commit are **green**. Human leftovers listed above.

Mode: **SOLO**. No scar.
